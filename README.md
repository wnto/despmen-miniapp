# despmen-miniapp

Static Telegram Mini App form for the DespMen expense bot.
Contains no secrets — only the entry form and category/payment-method names.
Data returns to the bot via `Telegram.WebApp.sendData`; this page never calls any API.

Deployed from the main (private) repo via `telegram-expenses-bot/miniapp/deploy.sh`. Do not edit here.
